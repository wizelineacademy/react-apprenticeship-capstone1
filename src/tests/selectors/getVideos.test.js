import React from 'react';
import { computeHeadingLevel, render } from '@testing-library/react';
import getVideos from '../../selectors/getVideos';

describe('', () => {
    test('should ', () => {
        getVideos().then( (videosData) => console.log)
        
    })
});
